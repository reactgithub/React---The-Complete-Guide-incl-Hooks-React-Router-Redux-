import React, { Component } from 'react';

// This is just an empty wrapper used for wrapping multiple JSX elements
// Since 16.2 there is a built in auxiliary wrapper called React.Fragment
const auxiliary = props => props.children;

export default auxiliary;